{% extends "layout.twig" %}

{% block content %}
    <section>
        {% for article in articles %}
            <article>
                <h2>{{ article.title }}</h2>
                <p class="body">
                    {{ article.body }}
                </p>
                <ul>
                    <p>Tags : </p>
                    <li><a href="#">{{ article.tag1 }}</a></li>
                    <li><a href="#">{{ article.tag2 }}</a></li>
                </ul>
                <div class="clear"></div>
            </article>

        {% endfor %}
    </section>
{% endblock %}
